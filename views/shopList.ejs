<h1 class="text-4xl mb-6"><%= title %></h1>

<ul>
  <% if (allShops.length) { %> <% for( let i = 0; i < allShops.length; i++ ) {
  %>
  <li>
    <a href="<%=`/categories/${allShops[i].url}`%>"> <%=allShops[i].name%></a>
  </li>
  <% } %> <% }else{ %>
  <li>There are no shops.</li>
  <%}%>
</ul>
